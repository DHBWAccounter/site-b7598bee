# Browser-facing Convex deployment URL used by the generated frontend.
# In local development, running `npx convex dev` will usually manage `.env.local` for you.
# Production Convex deploys are orchestrated by Krin, so this repo does not use CONVEX_DEPLOY_KEY.
NEXT_PUBLIC_CONVEX_URL=
